import { LazyCompDirective } from './lazy-comp.directive';

describe('LazyCompDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyCompDirective();
    expect(directive).toBeTruthy();
  });
});
